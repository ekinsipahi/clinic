services:
  - type: web
    name: devrim-web
    env: python
    buildCommand: ""
    startCommand: gunicorn myproject.wsgi:application
    autoDeploy: true
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: myproject.settings
      - key: PYTHON_VERSION
        value: 3.11
